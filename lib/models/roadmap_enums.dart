enum RoadmapNodeType {
  level,
  lesson,
  reward,
  milestone,
  sideQuest,
  bonus,
  mystery,
  mastery,
}

enum RewardType {
  xp,
  coins,
  collectible,
  badge,
  cosmetic,
  chest,
}

enum RewardRarity {
  common,
  rare,
  epic,
  legendary,
}

enum RoadmapNodeStatus {
  locked,
  available,
  current,
  completed,
  claimable,
}
